package ARRAYLIST;

import java.util.ArrayList;

public class Main {
//    import java.util.ArrayList;

//  Creating an ArrayList: ArrayList<Type> list = new ArrayList<>();

//   To add an element to the ArrayList, use the add() method.
//    list.add(element);

//    To access an element, use the get() method, passing the index as an argument.
//    list.get(index);
//    String firstName = names.get(0);

//    You can change an element at a specific index using the set() method.
//    list.set(index, element);
//    names.set(1, "Bob");  // Changes "Alice" to "Bob"

//    Use remove() to remove an element by index or by value.
//    list.remove(index);   // Removes element at index
//    list.remove(object);  // Removes the first occurrence of the object
//    names.remove(0);  // Removes "John"
//    names.remove("Bob");  // Removes "Bob"

//    Use contains() to check if the ArrayList contains a specific element.
//    list.contains(element);
//    boolean hasAlice = names.contains("Alice");  // Returns false (since Alice was changed to Bob)

//    The size() method returns the number of elements in the ArrayList.
//    list.size();
//    int numberOfNames = names.size();  // Returns the size of the ArrayList

//    You can iterate over an ArrayList using different methods such as a for loop, enhanced for loop, or iterator.
//    for (int i = 0; i < names.size(); i++) { // array iteration
//        System.out.println(names.get(i));
//    }
//    for (String name : names) {  string iteration
//        System.out.println(name);
//    }

//    To remove all elements from the ArrayList, use the clear() method.
//    list.clear();
//    names.clear();  // Removes all elements from the ArrayList


//        public static void main(String[] args) {
//            // Create an ArrayList
//            ArrayList<String> names = new ArrayList<>();
//
//            // Add elements
//            names.add("John");
//            names.add("Alice");
//            names.add("Bob");
//
//            // Access an element
//            System.out.println("First name: " + names.get(0));
//
//            // Modify an element
//            names.set(1, "Charlie");
//
//            // Remove an element
//            names.remove("John");
//
//            // Check if an element exists
//            if (names.contains("Bob")) {
//                System.out.println("Bob is in the list.");
//            }
//
//            // Size of the ArrayList
//            System.out.println("Size of the ArrayList: " + names.size());
//
//            // Iterating through the ArrayList
//            for (String name : names) {
//                System.out.println(name);
//            }
//
//            // Clear the ArrayList
//            names.clear();
//            System.out.println("ArrayList cleared, size: " + names.size());
//        }
    }









