package TIMESPACECOM;

public class second {
    public static void main(String[] args) {
        // Since this code leads to an infinite loop, time complexity is undefined. An infinite
        // loop means the algorithm will never terminate, and thus it doesn't make sense to compute
        // a traditional time complexity.
//        int c = 0;
//        for(int i = n; i > 1; i /= i) {
//            c++;
//        }
    }
}
